git init
git clear

Local project level
git config user.name
git config user.email

Globaly
git config -g user.name
git config -g user.email

git branch
git checkout

git checkout -b <branchname>

we can connect our local git repo to multiple remote repo
In general its only one remote repo per local repo

Name of remote repo[Key] -> "Link "
key ->origin


 git remote add origin https://github.com/vsvikasshrm7/GitDemo.git


raising a Pull Request PR